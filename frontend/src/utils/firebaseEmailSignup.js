/**
 * Barrel re-exports — import from `src/services/firebaseAuth.js` in new code.
 * @deprecated Use ../services/firebaseAuth.js
 */
export {
    mapFirebaseAuthError,
    firebaseEmailSignUpCreateUserOnly,
    startPhoneLink,
    firebaseCompleteSignUpAfterPhone,
} from '../services/firebaseAuth.js';
